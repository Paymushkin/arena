# Руководство по контрибуции

Спасибо за интерес к проекту The Arena! Мы приветствуем вклад от сообщества.

## Как начать

1. **Fork репозитория**

   ```bash
   # Нажмите кнопку "Fork" на GitHub
   git clone https://github.com/YOUR_USERNAME/arena.git
   cd arena
   ```

2. **Установите зависимости**

   ```bash
   npm install
   ```

3. **Создайте ветку для ваших изменений**
   ```bash
   git checkout -b feature/your-feature-name
   # или
   git checkout -b fix/your-bug-fix
   ```

## Типы контрибуций

### 🐛 Сообщения о багах

При обнаружении бага, пожалуйста:

1. Проверьте, нет ли уже открытого issue с этой проблемой
2. Создайте новый issue с описанием:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версия браузера и ОС

### ✨ Новые функции

Перед началом работы над новой функцией:

1. Откройте issue для обсуждения
2. Дождитесь одобрения от мейнтейнеров
3. Начинайте разработку

### 📝 Документация

Улучшения документации всегда приветствуются:

- Исправление опечаток
- Дополнение примеров
- Улучшение описаний

### 🎨 UI/UX улучшения

При работе с интерфейсом:

- Следуйте дизайн-системе проекта
- Обеспечьте адаптивность
- Проверьте доступность (a11y)

## Стандарты кода

### TypeScript

```typescript
// ✅ Хорошо
interface User {
  id: string
  name: string
  email: string
}

const getUser = async (id: string): Promise<User> => {
  const { data } = await useApi<User>(`/users/${id}`)
  return data.value
}

// ❌ Плохо
const getUser = async (id: any) => {
  const data = await useApi(`/users/${id}`)
  return data.value
}
```

### Vue компоненты

```vue
<!-- ✅ Хорошо -->
<script setup lang="ts">
interface Props {
  title: string
  count?: number
}

const props = withDefaults(defineProps<Props>(), {
  count: 0
})

const emit = defineEmits<{
  update: [count: number]
}>()
</script>

<template>
  <div class="card">
    <h2>{{ title }}</h2>
    <p>Count: {{ count }}</p>
  </div>
</template>
```

### Tailwind CSS

```vue
<!-- ✅ Хорошо -->
<button class="btn-primary">
  Click me
</button>

<div class="flex items-center justify-between p-4">
  <!-- content -->
</div>

<!-- ❌ Плохо (слишком много классов) -->
<div
  class="flex items-center justify-between p-4 bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300 border border-gray-200"
>
  <!-- Создайте переиспользуемый компонент или класс -->
</div>
```

## Процесс Pull Request

1. **Убедитесь, что код работает**

   ```bash
   npm run dev
   # Проверьте функциональность
   ```

2. **Проверьте линтер**

   ```bash
   npm run lint
   ```

3. **Соберите проект**

   ```bash
   npm run generate
   ```

4. **Commit с понятным сообщением**

   ```bash
   git add .
   git commit -m "feat: add user profile page"
   # или
   git commit -m "fix: resolve navigation menu issue on mobile"
   ```

   Формат commit сообщений:

   - `feat:` - новая функция
   - `fix:` - исправление бага
   - `docs:` - изменения в документации
   - `style:` - форматирование, отступы
   - `refactor:` - рефакторинг кода
   - `test:` - добавление тестов
   - `chore:` - обновление зависимостей и т.д.

5. **Push в ваш fork**

   ```bash
   git push origin feature/your-feature-name
   ```

6. **Создайте Pull Request**
   - Опишите изменения
   - Добавьте скриншоты (для UI изменений)
   - Упомяните связанные issues

## Структура PR

```markdown
## Описание

Краткое описание изменений

## Тип изменения

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Как проверить

1. Шаг 1
2. Шаг 2
3. ...

## Скриншоты (если применимо)

...

## Checklist

- [ ] Код следует стандартам проекта
- [ ] Проведен self-review
- [ ] Добавлены комментарии в сложных местах
- [ ] Обновлена документация
- [ ] Нет предупреждений линтера
- [ ] Проверена адаптивность (для UI)
```

## Code Review процесс

После создания PR:

1. Автоматические проверки должны пройти успешно
2. Хотя бы один мейнтейнер должен одобрить PR
3. Исправьте замечания, если они есть
4. После одобрения PR будет смержен

## Стайлгайд

### Именование

- **Компоненты**: PascalCase (`UserProfile.vue`)
- **Composables**: camelCase с префиксом `use` (`useAuth.ts`)
- **Utils**: camelCase (`formatDate.ts`)
- **Types**: PascalCase (`User`, `ApiResponse`)
- **Constants**: UPPER_SNAKE_CASE (`API_BASE_URL`)

### Структура файлов

```
component/
  FeatureName.vue
  FeatureName.spec.ts (если есть тесты)
  types.ts (специфичные типы)
  utils.ts (специфичные утилиты)
```

### Комментарии

```typescript
/**
 * Получает данные пользователя по ID
 * @param id - ID пользователя
 * @returns Promise с данными пользователя
 */
const getUser = async (id: string): Promise<User> => {
  // Реализация
}
```

## Тестирование

```typescript
// component.spec.ts
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import Component from './Component.vue'

describe('Component', () => {
  it('renders properly', () => {
    const wrapper = mount(Component, {
      props: { title: 'Test' }
    })
    expect(wrapper.text()).toContain('Test')
  })
})
```

## Доступность (a11y)

- Используйте семантический HTML
- Добавляйте `alt` для изображений
- Обеспечьте keyboard navigation
- Проверьте контрастность цветов
- Добавляйте ARIA атрибуты где необходимо

```vue
<!-- ✅ Хорошо -->
<button type="button" aria-label="Close dialog" @click="closeDialog">
  <svg>...</svg>
</button>

<!-- ❌ Плохо -->
<div @click="closeDialog">
  <svg>...</svg>
</div>
```

## Адаптивность

Проверьте на всех breakpoints:

- Mobile: 320px - 767px
- Tablet: 768px - 1023px
- Desktop: 1024px+

```vue
<!-- Используйте Tailwind responsive prefixes -->
<div class="text-sm md:text-base lg:text-lg">
  Responsive text
</div>
```

## Производительность

- Используйте lazy loading для компонентов
- Оптимизируйте изображения
- Избегайте ненужных re-renders
- Используйте `v-once` для статического контента

```vue
<script setup>
// Lazy loading
const HeavyComponent = defineAsyncComponent(() => import('./HeavyComponent.vue'))
</script>
```

## Получение помощи

- 📖 Читайте [документацию проекта](README.md)
- 💬 Создавайте issues для вопросов
- 📧 Email: dev@thearena.show

## Лицензия

Делая вклад в проект, вы соглашаетесь с тем, что ваш код будет лицензирован под той же лицензией, что и проект (MIT).

---

Спасибо за ваш вклад! 🎉
