# The Arena

Современное веб-приложение, построенное на Vue 3, Nuxt 3, Tailwind CSS и TypeScript.

## 🚀 Технологии

- **Vue 3** - прогрессивный JavaScript фреймворк
- **Nuxt 3** - метафреймворк для Vue.js
- **Tailwind CSS** - utility-first CSS фреймворк
- **TypeScript** - типизированный JavaScript
- **Pinia** - управление состоянием
- **GitHub Pages** - хостинг статических сайтов

## 📁 Структура проекта

```
arena/
├── assets/                 # Статические ассеты
│   ├── css/               # CSS файлы
│   ├── fonts/             # Шрифты
│   ├── images/            # Изображения
│   └── icons/             # SVG иконки
├── components/            # Vue компоненты
│   ├── icons/             # Компоненты иконок
│   └── ui/                # UI компоненты
├── composables/           # Композаблы (Vue Composition API)
├── constants/             # Константы приложения
├── layouts/               # Лейауты страниц
├── middleware/            # Middleware для маршрутизации
├── pages/                 # Страницы приложения
├── plugins/               # Nuxt плагины
├── public/                # Публичные файлы
├── scripts/               # Скрипты (деплой и т.д.)
├── stores/                # Pinia хранилища
├── types/                 # TypeScript типы
└── utils/                 # Утилитарные функции
```

## 🛠️ Установка

1. Клонируйте репозиторий:

```bash
git clone <your-repository-url>
cd arena
```

2. Установите зависимости:

```bash
npm install
```

3. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

## 💻 Разработка

Запустите dev сервер на `http://localhost:3000`:

```bash
npm run dev
```

## 🏗️ Сборка

Соберите приложение для production:

```bash
npm run generate
```

Это создаст статическую версию сайта в папке `.output/public`.

## 🚀 Деплой на GitHub Pages

Для деплоя на GitHub Pages используйте команду:

```bash
npm run deploy
```

Эта команда:

1. Создаст production сборку
2. Создаст файл `CNAME` с доменом `thearena.show`
3. Создаст файл `.nojekyll` для корректной работы
4. Опубликует результат в ветку `gh-pages`

### Настройка GitHub Pages

1. Перейдите в Settings → Pages вашего репозитория
2. В разделе Source выберите ветку `gh-pages`
3. В разделе Custom domain укажите `thearena.show`
4. Настройте DNS записи вашего домена:
   - Тип: CNAME
   - Имя: @
   - Значение: `<your-username>.github.io`

## 📝 Основные команды

- `npm run dev` - запустить dev сервер
- `npm run build` - собрать для production
- `npm run generate` - создать статическую версию
- `npm run preview` - предпросмотр production сборки
- `npm run deploy` - деплой на GitHub Pages
- `npm run lint` - проверить код линтером
- `npm run lint:fix` - исправить ошибки линтера

## 🎨 Tailwind CSS

Проект настроен с кастомной конфигурацией Tailwind CSS:

- Кастомные цвета (primary, secondary)
- Кастомные шрифты (Inter, Poppins)
- Расширенные размеры и spacing
- Утилитарные классы для кнопок, карточек и т.д.

## 📦 Компоненты

### Глобальные компоненты

- `AppHeader` - шапка сайта с навигацией
- `AppFooter` - подвал сайта

### UI компоненты

Создавайте переиспользуемые UI компоненты в папке `components/ui/`:

- Кнопки
- Инпуты
- Модальные окна
- Карточки
- И другие

## 🔧 Composables

Доступные composables:

- `useApi` - для работы с API
- `useToast` - для показа уведомлений
- Другие Nuxt composables: `useFetch`, `useRoute`, `useRouter`, и т.д.

## 💾 Управление состоянием

Используется Pinia для управления глобальным состоянием:

```typescript
import { useAppStore } from '~/stores/app'

const appStore = useAppStore()
appStore.setLoading(true)
```

## 🎯 TypeScript

Проект полностью типизирован. Типы находятся в папке `types/`.

Пример использования:

```typescript
import type { User, ApiResponse } from '~/types'

const user: User = {
  id: '1',
  name: 'John Doe',
  email: 'john@example.com'
}
```

## 📱 Адаптивность

Все компоненты адаптивны и оптимизированы для мобильных устройств благодаря Tailwind CSS.

## 🔍 SEO

Используйте `useSeoMeta` для настройки мета-тегов на каждой странице:

```typescript
useSeoMeta({
  title: 'Заголовок страницы',
  description: 'Описание страницы',
  ogImage: '/image.jpg'
})
```

## 📄 Лицензия

MIT

## 🤝 Контрибьюция

Вклад приветствуется! Пожалуйста, создавайте pull request'ы.

## 📞 Контакты

- Website: [thearena.show](https://thearena.show)
- GitHub: [github.com](https://github.com)

---

Сделано с ❤️ и Vue 3
